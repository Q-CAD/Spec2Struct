from setuptools import find_packages, setup

setup(
    name="atomdiff",
    version="0.0.1",
    description="diffusion over atomic positions",
    packages=find_packages(),
    include_package_data=True,
)